# Assessment 消费合同

单轨发现 Skill 不生成或重算单公司评分。只消费 `$geto-diligence-company` 写入 company.json 的 assessment。

跨公司排序仅允许 researchStatus 已完成、assessmentType=lead_value、六个维度完整且 modelVersion 一致的公司。其余候选保留在池中，报告具体缺口；不同模型版本不得直接混排。
